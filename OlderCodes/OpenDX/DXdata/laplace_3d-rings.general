file = /home/rubin/DXdata/laplace_3d-rings.dat
grid = 100 x 100 x 40
format = ascii
interleaving = record
majority = column
field = field0
structure = scalar
type = double
dependency = positions
positions = regular, regular, regular, 0, 1, 0, 1, 0, 1

end
